---
import Section from "./Section.astro";
import { services } from "../data/siteContent";

/**
 * Outcome-framed subtitles — maps service slug to a result-oriented label.
 * These replace the generic "(Forestry Mulching)" subtitle.
 */
const outcomeLabels: Record<string, string> = {
  "land-brush-clearing": "Usability & Access Restoration",
  "construction-lot-prep": "Development-Ready Site Preparation",
  "trails-access-corridors": "Property Access & Connectivity",
  "fence-line-clearing": "Boundary Visibility & Maintenance Access",
  "invasive-vegetation-removal": "Ecosystem Recovery & Land Restoration",
  "storm-property-reclamation": "Rapid Property Recovery",
};
---

<Section id="services" bg="bg-surface" noise>
  <!-- Section Header -->
  <div class="text-center mb-14">
    <p class="text-olive2 text-xs font-heading font-semibold uppercase tracking-[0.18em] mb-3">
      What We Deliver
    </p>
    <h2 class="text-3xl sm:text-4xl lg:text-5xl font-heading font-bold text-text">
      LAND &amp; BRUSH CLEARING
    </h2>
    <p class="mt-4 text-muted text-base max-w-2xl mx-auto leading-relaxed">
      Every project is scoped for usability, access, and long-term property optimization. We don't just clear land&mdash;we prepare it for what comes next.
    </p>
  </div>

  <!-- Grid -->
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">
    {services.map((service) => (
      <a
        id={service.anchorId}
        href={`/services/${service.slug}`}
        class="group block rounded-lg overflow-hidden bg-surface2 border border-border transition-all duration-[250ms] ease-out hover:-translate-y-1.5 hover:border-border-hover hover:shadow-[0_12px_40px_rgba(0,0,0,0.35),0_0_0_1px_rgba(200,163,105,0.15)] scroll-mt-24"
      >
        <!-- Image -->
        <div class="relative aspect-[16/10] overflow-hidden bg-gradient-to-br from-surface2 via-[#1a3028] to-surface">
          <img
            src={service.image}
            alt={service.title}
            class="w-full h-full object-cover opacity-90 group-hover:opacity-100 group-hover:scale-[1.03] transition-all duration-500"
            loading="lazy"
            onerror="this.style.display='none'"
          />
        </div>

        <!-- Content -->
        <div class="p-5 lg:p-6">
          <h3 class="font-heading font-bold text-base lg:text-lg text-text uppercase tracking-wide group-hover:text-accent transition-colors duration-200">
            {service.title}
            <span class="block text-olive2 text-xs font-semibold tracking-[0.08em] mt-1">
              {outcomeLabels[service.slug] || "Professional Land Transformation"}
            </span>
          </h3>
          <p class="mt-2 text-muted text-sm leading-relaxed">
            {service.description}
          </p>
        </div>
      </a>
    ))}
  </div>

  <!-- Method Note -->
  <p class="mt-10 text-center text-muted/60 text-xs italic max-w-2xl mx-auto leading-relaxed">
    All services use low-disturbance forestry mulching methodology&mdash;no burning, no hauling, no heavy excavation in most cases.
  </p>
</Section>
