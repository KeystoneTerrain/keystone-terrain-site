---
import BaseLayout from "../layouts/BaseLayout.astro";
import { site } from "../data/siteContent";
---

<BaseLayout
  title="About Us | Keystone Terrain Co."
  description="Learn about Keystone Terrain Co. — a locally owned forestry mulching and land clearing company serving the Greater Pittsburgh area from North Huntingdon, PA."
>
  <!-- Hero -->
  <section class="relative w-full noise-overlay overflow-hidden">
    <div class="absolute inset-0 bg-[radial-gradient(ellipse_80%_50%_at_50%_0%,#1a2e1f_0%,#0B1411_60%)]"></div>
    <div class="relative z-10 container-main pt-16 pb-14 lg:pt-24 lg:pb-20 text-center">
      <p class="text-olive2 text-xs font-heading font-semibold uppercase tracking-[0.18em] mb-3">
        About Us
      </p>
      <h1 class="text-3xl sm:text-4xl lg:text-5xl font-heading font-bold text-text leading-[1.08]">
        LOCALLY OWNED. BUILT FOR<br class="hidden sm:block" /> WESTERN PA TERRAIN.
      </h1>
    </div>
    <div class="absolute bottom-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-border to-transparent"></div>
  </section>

  <!-- Content -->
  <section class="w-full bg-surface py-16 lg:py-24">
    <div class="container-main max-w-[820px]">
      <div class="space-y-6 text-muted text-base leading-relaxed">
        <p>
          <strong class="text-text">Keystone Terrain Co.</strong> is a professional forestry mulching and land clearing company based in North Huntingdon, PA. We serve homeowners, contractors, and property managers throughout the Greater Pittsburgh area and surrounding counties.
        </p>
        <p>
          Our fleet includes professional-grade forestry mulching equipment — including remote-controlled units designed for steep hillsides and tough terrain that most companies turn down. We don't burn, we don't haul — we mulch everything in place, leaving your land clean, usable, and ready for the next step.
        </p>
        <p>
          Whether you need a few acres cleared for a build site, overgrown fence lines restored, invasive species removed, or ATV trails cut through dense woodland, we handle it efficiently and affordably — often completing residential jobs in a single day.
        </p>
      </div>

      <!-- Stats -->
      <div class="mt-14 grid grid-cols-2 sm:grid-cols-4 gap-6">
        {[
          { stat: "5★", label: "Google Rating" },
          { stat: "100%", label: "Insured" },
          { stat: "1 Day", label: "Most Jobs" },
          { stat: "60°+", label: "Slope Capable" },
        ].map((item) => (
          <div class="text-center">
            <p class="text-2xl sm:text-3xl font-heading font-bold text-accent">{item.stat}</p>
            <p class="mt-1 text-muted text-xs uppercase tracking-wider font-heading">{item.label}</p>
          </div>
        ))}
      </div>

      <!-- CTA -->
      <div class="mt-14 pt-10 border-t border-border text-center">
        <h2 class="text-2xl font-heading font-bold text-text">READY TO GET STARTED?</h2>
        <p class="mt-3 text-muted text-sm">
          Call us or use our interactive map tool to get a fast, accurate quote.
        </p>
        <div class="mt-6 flex flex-col sm:flex-row items-center justify-center gap-4">
          <a href="/quote" class="btn btn-primary">
            Map Your Property &amp; Get Quote
          </a>
          <a
            href={`tel:${site.phone.replace(/\D/g, "")}`}
            class="text-accent text-sm font-semibold hover:text-accent2 transition-colors"
          >
            {site.phone}
          </a>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
